package com.ebook.ebook.repo;

import com.ebook.ebook.entity.EBookChapter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EBookChapterRepository extends JpaRepository<EBookChapter,String> {
}
